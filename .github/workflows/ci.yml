name: CI Pipeline
on:
pull_request:
branches: [ master, main ]
jobs:
check-and-build:
runs-on: ubuntu-latest
steps:
- name: Checkout code
uses: actions/checkout@v4
- name: Build Docker Image
run: |
docker build -t freightmatch-demo.